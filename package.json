{
  "name": "sanati-workshop",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build && node scripts/prepare-sites-build.mjs"
  }
}

# Setup reference

The canonical installation, environment, Docker, testing, email, WebSocket, and Cloudinary instructions are maintained in [README.md](README.md).

For automated local setup:

- Windows: `setup.bat`
- Linux/macOS: `bash setup.sh`

Production requires PostgreSQL, Redis, SMTP credentials, a strong Django secret, allowed hosts/origins, and Cloudinary credentials.
